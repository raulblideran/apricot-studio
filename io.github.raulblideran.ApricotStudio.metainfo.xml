<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.raulblideran.ApricotStudio</id>

  <name>Apricot Studio</name>
  <summary>Trim a clip from a video without re-encoding decisions</summary>

  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>

  <developer id="io.github.raulblideran">
    <name>Raul Blideran</name>
  </developer>

  <description>
    <p>
      Apricot Studio does one thing: it takes a video, lets you mark a start and
      an end, and writes out that section. It is built for pulling a highlight
      out of a screen-recorder replay buffer without opening a full editor.
    </p>
    <p>
      There are no export settings to answer. Every parameter is read from the
      file you opened — container, codec, resolution, frame rate, colour, GOP
      structure and bitrate — so the clip behaves like the recording it came
      from. Audio is stream-copied rather than re-encoded, so it takes no loss
      at all.
    </p>
    <p>
      When the start of your selection lands on a keyframe, nothing needs to be
      decoded and the clip is copied straight out of the source: near-instant,
      and byte-for-byte identical. The app tells you which of the two paths an
      export will take before you run it.
    </p>
    <p>Features:</p>
    <ul>
      <li>Frame-exact cuts at both ends</li>
      <li>Instant, lossless export when the cut starts on a keyframe</li>
      <li>Timeline with a filmstrip, audio waveform and keyframe markers</li>
      <li>Quality presets that scale against the source, or a target file size</li>
      <li>WebM and GIF export</li>
      <li>Per-track audio selection for recordings with separate mic and desktop</li>
      <li>Optionally move the original to the wastebasket after exporting</li>
    </ul>
  </description>

  <launchable type="desktop-id">io.github.raulblideran.ApricotStudio.desktop</launchable>

  <url type="homepage">https://github.com/raulblideran/apricot-studio</url>
  <url type="bugtracker">https://github.com/raulblideran/apricot-studio/issues</url>
  <url type="vcs-browser">https://github.com/raulblideran/apricot-studio</url>

  <categories>
    <category>AudioVideo</category>
    <category>Video</category>
    <category>AudioVideoEditing</category>
  </categories>

  <keywords>
    <keyword>clip</keyword>
    <keyword>trim</keyword>
    <keyword>cut</keyword>
    <keyword>video</keyword>
    <keyword>ffmpeg</keyword>
  </keywords>

  <supports>
    <control>pointing</control>
    <control>keyboard</control>
  </supports>

  <requires>
    <display_length compare="ge">768</display_length>
  </requires>

  <content_rating type="oars-1.1"/>

  <releases>
    <release version="1.2.0" date="2026-08-19">
      <description>
        <p>
          Several exports were quietly not doing what the window said they were
          doing. Each of those is fixed, and each now has a test that was
          confirmed to fail without it.
        </p>
        <ul>
          <li>A GIF is cut from the clip alone. It used to read the rest of the file to build its palette — minutes and gigabytes of memory on a long recording, and colours taken from footage that was never in the clip</li>
          <li>WebM honours the quality preset and the size target. Both were ignored, while the estimate beside the button promised the size that had been asked for</li>
          <li>A size target no longer re-encodes a VP9 or AV1 source to H.264, which for a WebM source produced a file no player would accept</li>
          <li>The prompt offering to delete the original states the length of that file rather than of whichever one is open now</li>
          <li>Show in folder works for clips whose names contain spaces or punctuation</li>
          <li>A failed or cancelled export clears away the half-written clip it left behind, and never removes a file it did not write</li>
        </ul>
        <p>
          The window also remembers its size, the timeline follows the playhead
          when zoomed in, clicking the picture pauses, and very long recordings
          no longer fill memory with filmstrip frames. New icon.
        </p>
      </description>
    </release>
    <release version="1.1.0" date="2026-08-15">
      <description>
        <p>
          A video kept outside Videos, Pictures or Downloads used to be reported
          as missing, because a sandboxed app cannot see it at all. Apricot
          Studio now recognises that and says so.
        </p>
        <ul>
          <li>Locate the file to open it once, or copy the command that allows its folder for good</li>
          <li>Opening such a file from the file manager starts the app and explains, rather than appearing to fail</li>
          <li>Clips are written to Videos when the source folder cannot take them</li>
        </ul>
      </description>
    </release>
    <release version="1.0.0" date="2026-08-08">
      <description>
        <p>First release.</p>
      </description>
    </release>
  </releases>
</component>
